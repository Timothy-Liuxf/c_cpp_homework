/*#pragma warning (disable:4996)
#include <stdio.h>
struct date {
	int year;
	int month;
	int day;
};
int main()
{
	struct date d;
	scanf("%d%d%d", &d.year, &d.month, &d.day);
	printf("%d-%d-%d", d.year, d.month, d.day);
	return 0;
}*/